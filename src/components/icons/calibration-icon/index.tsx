import { Sliders } from "lucide-react-native";
import withDefaultProps from "../_withDefaultProps";

export default function CalibrationIcon(props: IconProps) {
  return withDefaultProps(Sliders)(props);
}
