import { BluetoothSearching } from "lucide-react-native";
import withDefaultProps from "../_withDefaultProps";

export default function BluetoothConnectIcon(props: IconProps) {
  return withDefaultProps(BluetoothSearching)(props);
}
