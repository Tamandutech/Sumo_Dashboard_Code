import { ActivitySquare } from "lucide-react-native";
import withDefaultProps from "../_withDefaultProps";

export default function ActivityIcon(props: IconProps) {
  return withDefaultProps(ActivitySquare)(props);
}
